Chiisana
	Middleware
		+ Implement caching for file and resources.
	+ Implement OWIN (http://owin.org/spec/owin-1.0.0.html)

Chiisana.Hosting.Self
	HttpContextRequest
		+ Where are Accept-Charset and Accept-Encoding?
	HttpContextResponse
		+ Fix the ability to remove headers that are generated by internal properties (incl. duplication of Date?).
		+ Add SendChunked - Gets or sets whether the response uses chunked transfer encoding.
	HttpServer
		+ Implement ReadTimeout for each asynchronous read operation (Not working at this point!). 

Chiisana.WebSocket
	+ Implement ping/pong to avoid time-out.


MiddlewareModule
	Handlers [HttpGet]
	Filters [Middleware]
	Model Binding (GET-querystring, POST-form or application/json)